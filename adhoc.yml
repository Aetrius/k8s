---
- name: join
  hosts: "{{ target }}"
  become: true
 
  tasks:
  - name: reset kubeadm
    shell: sudo kubeadm reset -f
    
  - name: join the nodes to the master
    shell: command-here
    with_items: "{{ groups['prox-k3s-workers'] }}"
